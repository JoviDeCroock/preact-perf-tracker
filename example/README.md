# Example app

Minimal Vite + Preact app that uses the local package build.

## Run

```bash
cd example
pnpm install
pnpm dev
```

The dependency is set to:

```json
"preact-perf-tracker": "file:.."
```

So changes in the parent package can be consumed locally.
